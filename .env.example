# Database Configuration

DB_USER=some-user
DB_PASSWORD=some-password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=talos_audit
TALOS_DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# API Security
TALOS_SECRET_KEY=dev_secret_key_change_me_in_production

TALOS_DB_USER=some_user
TALOS_DB_PASS=some_pass
TALOS_DB_HOST=localhost
TALOS_DB_PORT=5432
TALOS_DB_NAME=talos
# Database
DATABASE_URL=postgresql://${TALOS_DB_USER}:${TALOS_DB_PASS}@${TALOS_DB_HOST}:${TALOS_DB_PORT}/${TALOS_DB_NAME}

# Redis
REDIS_URL=redis://localhost:6379/0

# Security
MASTER_KEY=dev-master-key-change-in-prod

# OpenAI API Key (from root .env)
OPENAI_API_KEY=some-key

# Rate Limits
DEFAULT_RPM=60
DEFAULT_TPM=100000
